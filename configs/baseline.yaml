experiment:
  name: baseline_resnet18

seed: 42

dataset:
  name: cifar10
  data_dir: ./data
  num_classes: 10

model:
  name: resnet18

training:
  epochs: 100
  batch_size: 128

optimizer:
  name: sgd
  learning_rate: 0.1
  momentum: 0.9
  weight_decay: 0.0005

scheduler:
  name: cosine

augmentation:
  name: baseline
# Fixed across methods and training seeds.
validation:
  size: 5000
  seed: 2026
